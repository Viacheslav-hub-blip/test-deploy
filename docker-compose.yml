version: "3.9"

networks:
  dev:

services:
  nginx:
    build:
      context: ./nginx
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - dev

  backend:
    build:
      context: ./TestDeploy
    expose:
      - "8000"
    networks:
      - dev
